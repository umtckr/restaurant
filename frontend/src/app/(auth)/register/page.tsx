import { RegisterForm } from "@/components/auth/RegisterForm";

export const metadata = {
  title: "Create account — Dinebird",
  description: "Create your Dinebird account for restaurant operations.",
};

export default function RegisterPage() {
  return <RegisterForm />;
}
