import { LoginForm } from "@/components/auth/LoginForm";

export const metadata = {
  title: "Sign in — Dinebird",
  description: "Sign in to your restaurant operations workspace.",
};

export default function LoginPage() {
  return <LoginForm />;
}
