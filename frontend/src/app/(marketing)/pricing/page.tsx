import { PricingView } from "@/components/marketing/PricingView";

export const metadata = {
  title: "Pricing — Dinebird",
  description:
    "Flexible plans for single locations, multi-site groups, and enterprise restaurant platforms.",
};

export default function PricingPage() {
  return <PricingView />;
}
