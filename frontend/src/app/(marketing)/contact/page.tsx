import { ContactView } from "@/components/marketing/ContactView";

export const metadata = {
  title: "Contact — Dinebird",
  description:
    "Book a demo, ask about pricing, or reach sales and partnerships for Dinebird.",
};

export default function ContactPage() {
  return <ContactView />;
}
