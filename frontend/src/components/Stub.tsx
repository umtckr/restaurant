export function Stub({ title }: { title: string }) {
  return (
    <main style={{ padding: "1rem", fontFamily: "system-ui" }}>
      <h1>{title}</h1>
      <p>Placeholder — UI to be implemented.</p>
    </main>
  );
}
